{
  "name": "stock-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "server": "npm install && mkdir -p logs && pm2 start ecosystem.config.cjs --update-env || pm2 reload ecosystem.config.cjs --update-env",
    "server:stop": "pm2 stop stock-bot",
    "server:restart": "pm2 restart stock-bot --update-env",
    "server:logs": "pm2 logs stock-bot",
    "server:status": "pm2 status stock-bot",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^16.6.1",
    "telegraf": "^4.16.3",
    "yahoo-finance2": "^3.14.0"
  },
  "devDependencies": {
    "pm2": "^7.0.1"
  }
}
